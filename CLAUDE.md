# CLAUDE Project Instructions

Primary rules are defined in `docs/ai/RULES.md`.

Execution defaults:
- Use Windows-style commands (`.\build.cmd ...`).
- Prefer stable defaults and opt-in for risky behavior.
- Avoid default-on speculative hooks.

If the user explicitly asks for an override, apply it and note the deviation.
