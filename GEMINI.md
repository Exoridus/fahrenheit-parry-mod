# GEMINI Project Instructions

Read and follow `docs/ai/RULES.md` for all repository work.

Project defaults:
- canonical workflow entrypoint is `.\build.cmd`,
- prioritize reproducible/stable behavior over aggressive experimentation,
- keep docs and behavior in sync when changing workflows.

User instructions can override these rules; document any override clearly.

