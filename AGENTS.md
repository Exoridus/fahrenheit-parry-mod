# AGENTS (Codex)

Use `docs/ai/RULES.md` as the authoritative project rule set.

If a user request conflicts with `docs/ai/RULES.md`, follow the user request and document the override in your response.

Always prefer:
- `.\build.cmd` workflows,
- minimal stable changes,
- cleanup of temporary experimental paths after validation.

