{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Exoridus/fahrenheit-parry-mod/docs/dev-local.schema.json",
  "title": "Local Developer Config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "GAME_DIR": {
      "type": "string",
      "description": "Absolute path to game install directory containing FFX.exe."
    },
    "AUTO_DEPLOY": {
      "type": "boolean",
      "description": "Enable automatic deployment after local builds."
    },
    "DEPLOY_MODE": {
      "type": "string",
      "enum": ["update", "replace", "mod-only"],
      "description": "Automatic deploy behavior for full builds."
    }
  },
  "required": ["GAME_DIR", "AUTO_DEPLOY", "DEPLOY_MODE"],
  "examples": [
    {
      "GAME_DIR": "C:\\Games\\Final Fantasy X-X2 - HD Remaster",
      "AUTO_DEPLOY": true,
      "DEPLOY_MODE": "update"
    }
  ]
}
